# Visualization playback

Mode: Operate. Scope: existing visualization modal only. Code-first, confirmed by user.

First viewport: compact full-question header with close action; one unboxed strip of named motion values; animation is the principal surface. Mimir paper, green accent, Geist, and both themes remain. Horizontal motion uses a tighter data viewport; falling motion keeps its vertical extent.

Signature interaction: autoplay and scrubbing directly connect object position to live numeric readouts. Existing eight-second playback, pause, adjacent-frame stepping, restart, request cancellation, and focus restoration remain. Centered transport controls sit above the timeline; elapsed and total simulated time replace the redundant percentage.

Motion grammar: functional continuous interpolation only; no decorative entrances. Compact layouts retain 44px transport targets and readable values. Preserve full question access, loading, error, and empty selection. No new product claims or API changes.
